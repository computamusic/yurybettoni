// React's `<ViewTransition>` ships in the canary types (used by Next's App
// Router). This reference loads those declarations so `import { ViewTransition }
// from "react"` typechecks against the stable @types/react.
/// <reference types="react/canary" />
